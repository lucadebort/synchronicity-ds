---
title: Checkbox
description: Selection control for multiple choices
---

import { Aside } from '@astrojs/starlight/components';
import PreviewBox from '../../../components/PreviewBox.astro';

# Checkbox

Checkbox allows users to select one or more items from a set. Supports checked, unchecked, and indeterminate states.

## Preview

<PreviewBox defaultTheme="dark" class="flex-row gap-xl p-lg">
  <div class="sync-checkbox unchecked"></div>
  <div class="sync-checkbox checked">
    <span class="checkmark">✓</span>
  </div>
  <div class="sync-checkbox indeterminate">
    <span class="dash">−</span>
  </div>
</PreviewBox>

---

## States

### Unchecked

<PreviewBox class="flex-row gap-lg p-lg">
  <div class="checkbox-demo">
    <div class="sync-checkbox unchecked"></div>
    <span class="checkbox-label">Unchecked</span>
  </div>
</PreviewBox>

```tsx
<Checkbox
  checked={false}
  onCheckedChange={setChecked}
  accessibilityLabel="Option"
/>
```

### Checked

<PreviewBox class="flex-row gap-lg p-lg">
  <div class="checkbox-demo">
    <div class="sync-checkbox checked">
      <span class="checkmark">✓</span>
    </div>
    <span class="checkbox-label">Checked</span>
  </div>
</PreviewBox>

```tsx
<Checkbox
  checked={true}
  onCheckedChange={setChecked}
  accessibilityLabel="Option"
/>
```

### Indeterminate

Used for "select all" when some children are selected.

<PreviewBox class="flex-row gap-lg p-lg">
  <div class="checkbox-demo">
    <div class="sync-checkbox indeterminate">
      <span class="dash">−</span>
    </div>
    <span class="checkbox-label">Indeterminate</span>
  </div>
</PreviewBox>

```tsx
<Checkbox
  checked={someChecked}
  indeterminate={!allChecked && someChecked}
  onCheckedChange={toggleAll}
  accessibilityLabel="Select all"
/>
```

---

## Sizes

<PreviewBox class="flex-row gap-xl p-lg align-center">
  <div class="checkbox-demo">
    <div class="sync-checkbox sm checked">
      <span class="checkmark sm">✓</span>
    </div>
    <span class="checkbox-label size">sm</span>
  </div>
  <div class="checkbox-demo">
    <div class="sync-checkbox md checked">
      <span class="checkmark md">✓</span>
    </div>
    <span class="checkbox-label size">md</span>
  </div>
  <div class="checkbox-demo">
    <div class="sync-checkbox lg checked">
      <span class="checkmark lg">✓</span>
    </div>
    <span class="checkbox-label size">lg</span>
  </div>
</PreviewBox>

| Size | Box | Usage |
|------|-----|-------|
| `sm` | 16px | Compact lists, tables |
| `md` | 20px | Default |
| `lg` | 24px | Prominent selections |

---

## Disabled

<PreviewBox class="flex-row gap-xl p-lg">
  <div class="checkbox-demo">
    <div class="sync-checkbox unchecked disabled"></div>
    <span class="checkbox-label disabled">Disabled</span>
  </div>
  <div class="checkbox-demo">
    <div class="sync-checkbox checked disabled">
      <span class="checkmark">✓</span>
    </div>
    <span class="checkbox-label disabled">Disabled checked</span>
  </div>
</PreviewBox>

```tsx
<Checkbox
  checked={false}
  disabled
  onCheckedChange={() => {}}
  accessibilityLabel="Unavailable option"
/>
```

---

## API Reference

### Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `checked` | `boolean` | **Required** | Whether checked |
| `onCheckedChange` | `(checked: boolean) => void` | **Required** | Change handler |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Checkbox size |
| `disabled` | `boolean` | `false` | Disable interaction |
| `indeterminate` | `boolean` | `false` | Indeterminate state |
| `style` | `ViewStyle` | - | Custom styles |
| `accessibilityLabel` | `string` | **Required** | A11y label |

---

## Complete Example

```tsx
import { Checkbox, Card, Text } from '@synchronicity/react-native';
import { View, StyleSheet } from 'react-native';
import { useState } from 'react';

function TrigramFilter() {
  const [selected, setSelected] = useState<string[]>([]);

  const trigrams = [
    { id: 'heaven', name: 'Heaven', symbol: '☰' },
    { id: 'earth', name: 'Earth', symbol: '☷' },
    { id: 'thunder', name: 'Thunder', symbol: '☳' },
    { id: 'water', name: 'Water', symbol: '☵' },
    { id: 'mountain', name: 'Mountain', symbol: '☶' },
    { id: 'wind', name: 'Wind', symbol: '☴' },
    { id: 'fire', name: 'Fire', symbol: '☲' },
    { id: 'lake', name: 'Lake', symbol: '☱' },
  ];

  const toggleTrigram = (id: string) => {
    setSelected(prev =>
      prev.includes(id)
        ? prev.filter(t => t !== id)
        : [...prev, id]
    );
  };

  const allSelected = selected.length === trigrams.length;
  const someSelected = selected.length > 0 && !allSelected;

  return (
    <Card>
      <View style={styles.header}>
        <Checkbox
          checked={allSelected}
          indeterminate={someSelected}
          onCheckedChange={(checked) => {
            setSelected(checked ? trigrams.map(t => t.id) : []);
          }}
          accessibilityLabel="Select all trigrams"
        />
        <Text style={styles.headerText}>Filter by Trigram</Text>
      </View>

      {trigrams.map(trigram => (
        <View key={trigram.id} style={styles.option}>
          <Checkbox
            checked={selected.includes(trigram.id)}
            onCheckedChange={() => toggleTrigram(trigram.id)}
            accessibilityLabel={`Select ${trigram.name}`}
          />
          <Text style={styles.symbol}>{trigram.symbol}</Text>
          <Text style={styles.name}>{trigram.name}</Text>
        </View>
      ))}
    </Card>
  );
}

const styles = StyleSheet.create({
  header: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
    paddingBottom: 16,
    borderBottomWidth: 1,
    borderBottomColor: '#3a3a48',
  },
  headerText: {
    fontSize: 16,
    fontWeight: '600',
  },
  option: {
    flexDirection: 'row',
    alignItems: 'center',
    gap: 12,
    paddingVertical: 8,
  },
  symbol: {
    fontSize: 20,
  },
  name: {
    fontSize: 14,
  },
});
```

<style>{`
  .sync-checkbox {
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid #5a5a68;
    border-radius: 5px;
    cursor: pointer;
    transition: all 0.15s ease;
  }

  .sync-checkbox:not(.sm):not(.lg) {
    width: 20px;
    height: 20px;
  }

  .sync-checkbox.sm { width: 16px; height: 16px; border-radius: 4px; }
  .sync-checkbox.lg { width: 24px; height: 24px; border-radius: 6px; }

  .sync-checkbox.checked,
  .sync-checkbox.indeterminate {
    background: #d4af37;
    border-color: #d4af37;
  }

  .sync-checkbox.disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  .checkmark, .dash {
    color: #1a1a24;
    font-weight: 700;
    font-size: 14px;
    line-height: 1;
  }

  .checkmark.sm, .dash.sm { font-size: 10px; }
  .checkmark.lg, .dash.lg { font-size: 16px; }

  .checkbox-demo {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  .checkbox-label {
    font-size: 14px;
    color: #e8e6e3;
  }

  .checkbox-label.size {
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    color: #8a8a9a;
  }

  .checkbox-label.disabled {
    opacity: 0.5;
  }

  /* Light theme */
  .preview-content[data-theme="light"] .sync-checkbox {
    border-color: #b0a89e;
  }

  .preview-content[data-theme="light"] .sync-checkbox.checked,
  .preview-content[data-theme="light"] .sync-checkbox.indeterminate {
    background: #886a00;
    border-color: #886a00;
  }

  .preview-content[data-theme="light"] .checkmark,
  .preview-content[data-theme="light"] .dash {
    color: white;
  }

  .preview-content[data-theme="light"] .checkbox-label {
    color: #2a2520;
  }

  .preview-content[data-theme="light"] .checkbox-label.size {
    color: #6b5f55;
  }
`}</style>
