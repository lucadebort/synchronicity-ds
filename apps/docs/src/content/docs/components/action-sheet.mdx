---
title: ActionSheet
description: Bottom action menu with options
---

import { Aside } from '@astrojs/starlight/components';
import PreviewBox from '../../../components/PreviewBox.astro';


ActionSheet presents a set of actions in a modal sheet from the bottom of the screen. Use for contextual actions, confirmations, or choices.

## Preview

<PreviewBox defaultTheme="dark" class="p-lg">
  <div class="action-sheet-demo">
    <div class="sheet-backdrop"></div>
    <div class="sheet-content">
      <div class="sheet-options">
        <div class="sheet-header">
          <span class="sheet-title">READING OPTIONS</span>
        </div>
        <div class="sheet-option">Share Reading</div>
        <div class="sheet-option">Save to Journal</div>
        <div class="sheet-option destructive">Delete Reading</div>
      </div>
      <div class="sheet-cancel">Cancel</div>
    </div>
  </div>
</PreviewBox>

---

## Basic Usage

<PreviewBox class="p-lg">
  <div class="action-sheet-demo small">
    <div class="sheet-content">
      <div class="sheet-options">
        <div class="sheet-option">Edit</div>
        <div class="sheet-option">Duplicate</div>
        <div class="sheet-option">Archive</div>
      </div>
      <div class="sheet-cancel">Cancel</div>
    </div>
  </div>
</PreviewBox>

```tsx
const [showActions, setShowActions] = useState(false);

<ActionSheet
  visible={showActions}
  onClose={() => setShowActions(false)}
  options={[
    { label: 'Edit', onPress: handleEdit },
    { label: 'Duplicate', onPress: handleDuplicate },
    { label: 'Archive', onPress: handleArchive },
  ]}
/>
```

---

## With Title and Message

<PreviewBox class="p-lg">
  <div class="action-sheet-demo">
    <div class="sheet-content">
      <div class="sheet-options">
        <div class="sheet-header">
          <span class="sheet-title">SHARE READING</span>
          <span class="sheet-message">Choose how you'd like to share this reading</span>
        </div>
        <div class="sheet-option">Copy Link</div>
        <div class="sheet-option">Share to Messages</div>
        <div class="sheet-option">Export as Image</div>
      </div>
      <div class="sheet-cancel">Cancel</div>
    </div>
  </div>
</PreviewBox>

```tsx
<ActionSheet
  visible={showShare}
  onClose={() => setShowShare(false)}
  title="Share Reading"
  message="Choose how you'd like to share this reading"
  options={[
    { label: 'Copy Link', onPress: copyLink },
    { label: 'Share to Messages', onPress: shareMessages },
    { label: 'Export as Image', onPress: exportImage },
  ]}
/>
```

---

## Destructive Actions

Use the `destructive` property for dangerous actions like delete.

<PreviewBox class="p-lg">
  <div class="action-sheet-demo small">
    <div class="sheet-content">
      <div class="sheet-options">
        <div class="sheet-header">
          <span class="sheet-title">DELETE READING?</span>
          <span class="sheet-message">This action cannot be undone</span>
        </div>
        <div class="sheet-option destructive">Delete Permanently</div>
      </div>
      <div class="sheet-cancel">Cancel</div>
    </div>
  </div>
</PreviewBox>

```tsx
<ActionSheet
  visible={showDelete}
  onClose={() => setShowDelete(false)}
  title="Delete Reading?"
  message="This action cannot be undone"
  options={[
    {
      label: 'Delete Permanently',
      onPress: handleDelete,
      destructive: true
    },
  ]}
/>
```

---

## With Icons

<PreviewBox class="p-lg">
  <div class="action-sheet-demo small">
    <div class="sheet-content">
      <div class="sheet-options">
        <div class="sheet-option with-icon"><span class="option-icon">📤</span> Share</div>
        <div class="sheet-option with-icon"><span class="option-icon">📋</span> Copy</div>
        <div class="sheet-option with-icon"><span class="option-icon">💾</span> Save</div>
      </div>
      <div class="sheet-cancel">Cancel</div>
    </div>
  </div>
</PreviewBox>

```tsx
<ActionSheet
  visible={showActions}
  onClose={() => setShowActions(false)}
  options={[
    { label: 'Share', onPress: share, icon: <ShareIcon /> },
    { label: 'Copy', onPress: copy, icon: <CopyIcon /> },
    { label: 'Save', onPress: save, icon: <SaveIcon /> },
  ]}
/>
```

---

## Disabled Options

<PreviewBox class="p-lg">
  <div class="action-sheet-demo small">
    <div class="sheet-content">
      <div class="sheet-options">
        <div class="sheet-option">Free Feature</div>
        <div class="sheet-option disabled">Premium Feature</div>
        <div class="sheet-option disabled">Team Feature</div>
      </div>
      <div class="sheet-cancel">Cancel</div>
    </div>
  </div>
</PreviewBox>

```tsx
<ActionSheet
  visible={showUpgrade}
  onClose={() => setShowUpgrade(false)}
  options={[
    { label: 'Free Feature', onPress: useFreature },
    { label: 'Premium Feature', onPress: usePremium, disabled: !isPremium },
    { label: 'Team Feature', onPress: useTeam, disabled: !isTeam },
  ]}
/>
```

---

## API Reference

### ActionSheet Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `visible` | `boolean` | **Required** | Whether visible |
| `onClose` | `() => void` | **Required** | Close handler |
| `options` | `ActionSheetOption[]` | **Required** | Action options |
| `title` | `string` | - | Sheet title |
| `message` | `string` | - | Sheet message |
| `cancelLabel` | `string` | `'Cancel'` | Cancel button text |
| `style` | `ViewStyle` | - | Custom styles |

### ActionSheetOption

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `label` | `string` | **Required** | Option text |
| `onPress` | `() => void` | **Required** | Press handler |
| `destructive` | `boolean` | `false` | Destructive styling |
| `disabled` | `boolean` | `false` | Disabled state |
| `icon` | `ReactNode` | - | Option icon |

---

## Complete Example

```tsx
import { ActionSheet, IconButton, Card } from '@synchronicity/react-native';
import { View, StyleSheet } from 'react-native';
import { useState } from 'react';

function ReadingCard({ reading, onDelete, onShare }) {
  const [showActions, setShowActions] = useState(false);

  const handleDelete = () => {
    Alert.alert(
      'Delete Reading',
      'Are you sure you want to delete this reading?',
      [
        { text: 'Cancel', style: 'cancel' },
        { text: 'Delete', style: 'destructive', onPress: onDelete },
      ]
    );
  };

  return (
    <>
      <Card>
        <View style={styles.header}>
          <Text style={styles.title}>{reading.hexagram.name}</Text>
          <IconButton
            icon="more"
            variant="ghost"
            onPress={() => setShowActions(true)}
            accessibilityLabel="More options"
          />
        </View>
        <Text style={styles.date}>{reading.date}</Text>
      </Card>

      <ActionSheet
        visible={showActions}
        onClose={() => setShowActions(false)}
        title="Reading Options"
        options={[
          {
            label: 'Share Reading',
            onPress: onShare,
            icon: <ShareIcon />,
          },
          {
            label: 'Add to Favorites',
            onPress: () => addToFavorites(reading.id),
            icon: <HeartIcon />,
          },
          {
            label: 'Export as PDF',
            onPress: () => exportPDF(reading),
            icon: <FileIcon />,
          },
          {
            label: 'Delete Reading',
            onPress: handleDelete,
            destructive: true,
            icon: <TrashIcon />,
          },
        ]}
      />
    </>
  );
}

const styles = StyleSheet.create({
  header: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  title: {
    fontSize: 18,
    fontWeight: '600',
  },
  date: {
    fontSize: 14,
    color: '#8a8a9a',
    marginTop: 4,
  },
});
```

<style>{`
  .action-sheet-demo {
    position: relative;
    width: 100%;
    height: 280px;
    border-radius: 12px;
    overflow: hidden;
    background: #1a1a24;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
  }

  .action-sheet-demo.small {
    height: 220px;
  }

  .sheet-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.5);
  }

  .sheet-content {
    position: relative;
    padding: 8px;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }

  .sheet-options {
    background: #2a2a38;
    border-radius: 12px;
    overflow: hidden;
  }

  .sheet-header {
    padding: 16px;
    text-align: center;
    border-bottom: 1px solid #3a3a48;
  }

  .sheet-title {
    display: block;
    font-size: 12px;
    font-weight: 500;
    color: #8a8a9a;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .sheet-message {
    display: block;
    font-size: 13px;
    color: #5a5a68;
    margin-top: 4px;
  }

  .sheet-option {
    padding: 16px;
    text-align: center;
    font-size: 16px;
    color: #e8e6e3;
    border-top: 1px solid #3a3a48;
    cursor: pointer;
  }

  .sheet-option:first-child,
  .sheet-header + .sheet-option {
    border-top: none;
  }

  .sheet-option.destructive {
    color: #ef4444;
  }

  .sheet-option.disabled {
    color: #5a5a68;
    opacity: 0.5;
  }

  .sheet-option.with-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
  }

  .option-icon {
    font-size: 18px;
  }

  .sheet-cancel {
    background: #2a2a38;
    border-radius: 12px;
    padding: 16px;
    text-align: center;
    font-size: 16px;
    font-weight: 600;
    color: #d4af37;
    cursor: pointer;
  }

  /* Light theme */
  .preview-content[data-theme="light"] .action-sheet-demo {
    background: #f5f3ee;
  }

  .preview-content[data-theme="light"] .sheet-options,
  .preview-content[data-theme="light"] .sheet-cancel {
    background: #ffffff;
  }

  .preview-content[data-theme="light"] .sheet-header {
    border-bottom-color: #eeebe2;
  }

  .preview-content[data-theme="light"] .sheet-option {
    border-top-color: #eeebe2;
    color: #2a2520;
  }

  .preview-content[data-theme="light"] .sheet-cancel {
    color: #886a00;
  }

  .preview-content[data-theme="light"] .sheet-title,
  .preview-content[data-theme="light"] .sheet-message {
    color: #6b5f55;
  }
`}</style>
